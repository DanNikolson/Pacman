// return allCells;

function getNonWalls(){
    // let allCellsArray = Object.entries(allCells);
    for (let index = 0; index === cellTeg.className != "Wall"; index++) {
        const element = array[index];
        
    }
}

NPCmanager = () =>{
    Bat = { x: 1, y: 38 };
    Ghost = { x: 1, y: 1 };
    Squid = { x: 1, y: 1 };
};